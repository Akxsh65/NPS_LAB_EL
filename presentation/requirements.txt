# Presentation demo + inference API (presentation/api_server.py)
# Install from repo root:
#   pip install -r presentation/requirements.txt
#
# Python 3.10 or 3.11 recommended. CPU-only PyTorch is fine.

torch>=2.2.0,<3
numpy>=1.26.0,<3
joblib>=1.3.0
scikit-learn>=1.4.0
tqdm>=4.66.0
